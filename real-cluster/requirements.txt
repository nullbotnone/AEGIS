# Real Cluster Dependencies
numpy>=1.24.0
h5py>=3.8.0
pyyaml>=6.0
requests>=2.28.0

# eBPF monitoring (optional, requires root)
# bcc>=0.26.0  # Install via: apt-get install python3-bcc

# Baseline comparisons
# tcpdump/tshark for DLP baseline (system package)
# auditd for filesystem auditing baseline (system package)
# statsmodels for behavioral analytics
statsmodels>=0.14.0

# Analysis and visualization
matplotlib>=3.6.0
pandas>=2.0.0
